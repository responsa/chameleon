<?xml version="1.0" encoding="UTF-8"?>
<root>
  <% @widget.items.each do |item| %>
  <item>
    <text><![CDATA[<%= item[:text] %>]]></text>
    <type><%=
      case item[:type]
        when :alert then 1
        when :info then 2
        else 0
      end
    %></type>
  </item>
  <% end %>
</root>
